import type { PartialOptions } from "overlayscrollbars";

export const SCROLLBAR_OPTIONS: PartialOptions = {
  scrollbars: {
    theme: "os-theme-pg",
    autoHide: "leave",
    autoHideDelay: 0,
    dragScroll: true,
  },
};
