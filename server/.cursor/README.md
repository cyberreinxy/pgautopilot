# Cursor

Config: `.cursor/mcp.json`

```json
{
  "mcpServers": {
    "pgautopilot": { "command": "pgautopilot" }
  }
}
```

Open Cursor in this folder → Settings → MCP → it should appear. If not, add it manually with the same `command`.
