{
    "servers": {
        "pgautopilot": {
            "type": "stdio",
            "command": "npx",
            "args": ["tsx", "src/index.ts"],
            "cwd": "${workspaceFolder}/server"
        }
    }
}